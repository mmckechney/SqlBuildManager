name: .NET Core Build

on:
  push:
    branches: 
      - master

  pull_request:
    branches:
      - master

env:
  DOTNET_VERSION: 'net10.0'
  DOTNET_FRAMEWORK_VERSION: '10.x'  

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v6

    - name: Setup .NET ${{ env.DOTNET_FRAMEWORK_VERSION }}
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: ${{ env.DOTNET_FRAMEWORK_VERSION }}

    - name: Restore locked dependencies
      run: |
        dotnet restore ./src/SqlBuildManager-console.sln --locked-mode -p:ContinuousIntegrationBuild=true
        dotnet restore ./src/SqlBuildManager.DbInformation.UnitTest/SqlBuildManager.DbInformation.UnitTest.csproj --locked-mode -p:ContinuousIntegrationBuild=true

    - name: Build console app
      run: dotnet build ./src/SqlBuildManager.Console/sbm.csproj --configuration Release -f ${{ env.DOTNET_VERSION }} --no-restore

    # Run tests
    - name: Run SqlBuildManager.Console.UnitTest
      run: dotnet test ./src/SqlBuildManager.Console.UnitTest/SqlBuildManager.Console.UnitTest.csproj --configuration Release --no-restore

    - name: Run SqlBuildManager.Enterprise.UnitTest
      run: dotnet test ./src/SqlBuildManager.Enterprise.UnitTest/SqlBuildManager.Enterprise.UnitTest.csproj --configuration Release --no-restore

    - name: Run SqlBuildManager.ScriptHandling.UnitTest
      run: dotnet test ./src/SqlBuildManager.ScriptHandling.UnitTest/SqlBuildManager.ScriptHandling.UnitTest.csproj --configuration Release --no-restore

    - name: Run SqlBuildManager.Connection.UnitTest
      run: dotnet test ./src/SqlBuildManager.Connection.UnitTest/SqlBuildManager.Connection.UnitTest.csproj --configuration Release --no-restore

    - name: Run SqlBuildManager.DbInformation.UnitTest
      run: dotnet test ./src/SqlBuildManager.DbInformation.UnitTest/SqlBuildManager.DbInformation.UnitTest.csproj --configuration Release --no-restore

    - name: Run SqlBuildManager.ObjectScript.UnitTest
      run: dotnet test ./src/SqlBuildManager.ObjectScript.UnitTest/SqlBuildManager.ObjectScript.UnitTest.csproj --configuration Release --no-restore

    - name: Run SqlBuildManager.SqlBuild.UnitTest
      run: dotnet test ./src/SqlBuildManager.SqlBuild.UnitTest/SqlBuildManager.SqlBuild.UnitTest.csproj --configuration Release --no-restore

    # Pubish
    - name: Publish the Windows CLI app 
      run: dotnet publish ./src/SqlBuildManager.Console/sbm.csproj -r win-x64 --configuration Release -f ${{ env.DOTNET_VERSION }} --self-contained

    - name: Publish the Linux CLI app
      run: dotnet publish ./src/SqlBuildManager.Console/sbm.csproj -r linux-x64 --configuration Release -f ${{ env.DOTNET_VERSION }} --self-contained

    # Artifacts
    - name: Upload a Build Artifact sbm-windows
      uses: actions/upload-artifact@v6
      with:
        name: sbm-windows-${{ env.DOTNET_VERSION }}
        path: "./src/SqlBuildManager.Console/bin/Release/${{ env.DOTNET_VERSION }}/win-x64/publish"

    - name: Upload a Build Artifact sbm-linux
      uses: actions/upload-artifact@v6
      with:
        name: sbm-linux-${{ env.DOTNET_VERSION }}
        path: "./src/SqlBuildManager.Console/bin/Release/${{ env.DOTNET_VERSION }}/linux-x64/publish"
    
