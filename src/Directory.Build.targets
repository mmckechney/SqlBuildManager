<Project>
  <!-- Suppress warnings in test projects. Uses .targets so it appends AFTER csproj NoWarn values. -->
  <PropertyGroup Condition="$(MSBuildProjectName.Contains('Test'))">
    <NoWarn>$(NoWarn);CS8604;CS8602;MSTEST0005;MSTEST0032</NoWarn>
  </PropertyGroup>
</Project>
